# Jewellery online store

Проект содержит исходный HTML-макет и миграцию на Angular 19.

## Что уже сделано

- Историческая версия (HTML/Gulp) сохранена в корне репозитория.
- Добавлено Angular 19 приложение в `angular-app/`.
- Подключена runtime-локализация интерфейса: `ru`, `en`, `es`.
- Реализованы роуты-страницы:
  - `/` — главная
  - `/catalog` — каталог
  - `/login` — вход
- Добавлены стартовые секции, приближенные к legacy-структуре (hero, категории, фильтры, карточки, форма входа).
- Выполнен 1:1 перенос ключевых блоков для страниц Home/Catalog/Login с legacy-классами (`hero`, `latest`, `catalog`, `filter`, `results`, `login`, `form`).
- Добавлено базовое состояние корзины (cart counter в header + добавление из каталога).

## Запуск Angular-версии

```bash
cd angular-app
npm install
npm start
```

## Следующие шаги миграции

1. Завершить 1:1 перенос оставшихся секций (footer, popup/cart modal, FAQ/blog/follow).
2. Подключить реальные данные каталога из API и состояние корзины к backend.
3. Реализовать полноценный поток авторизации/регистрации и guards.
